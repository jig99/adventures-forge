[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Serve index.html for any route so page refreshes don't 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
