// ============================================================
//  PASTE YOUR ADVENTURER'S FORGE CODE INTO THIS FILE
// ------------------------------------------------------------
//  1. Open the Adventurer's Forge artifact in Claude.
//  2. Copy the ENTIRE artifact code (all of it).
//  3. Delete everything in this file and paste the code in.
//  4. Keep the default export at the bottom. The artifact ends
//     with something like:  export default function App() {...}
//     Leave that line — main.jsx imports it as the default.
//
//  You do NOT need to touch any window.storage calls. The shim
//  in storage-shim.js makes them save to the browser instead.
//
//  If the artifact imports an extra library (e.g. lucide-react),
//  run:  npm install <that-library>
//  (The Forge as built uses only React + inline SVG, so this is
//   usually unnecessary.)
// ============================================================

export default function App() {
  return (
    <div style={{ padding: 40, fontFamily: 'system-ui, sans-serif', lineHeight: 1.5 }}>
      <h1>Paste your Adventurer's Forge code here</h1>
      <p>
        Open <code>src/AdventurersForge.jsx</code>, delete this placeholder, and paste your
        full artifact code. Then run <code>npm install</code> and <code>npm run build</code>.
      </p>
    </div>
  );
}
